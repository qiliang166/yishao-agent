Generate a complete set of prompt configurations for:

Industry/Topic: {{industry_topic}}
Purpose/Use Case: {{purpose_description}}

Here are the reference configurations to adapt:

```json
{{ref_json}}
```

Replace all industry-specific terms (currently about "食谱/菜品/烹饪/美食") with terms relevant to "{{industry_topic}}".
Keep all structural elements identical — only change the domain-specific content.
For col3, generate an 8-page document structure using these page types: cover, toc, content, table, chart, diagram, flowchart, closing.

Output ONLY the complete JSON with all 5+3+3+33 = 44 config entries.
