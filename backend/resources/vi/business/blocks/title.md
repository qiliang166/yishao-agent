# 标题 — 文档主标题行，hero 渐变背景 + accent 下划线

> 标题是文档的第一视觉锚点，必须醒目且可识别。所有文档模板均包含此段落。

---

## 结构规则

- **列数**: 全宽合并（colspan = 文档总列数）
- **背景**: `linear-gradient(135deg, {{primary}} 0%, {{secondary}} 100%)`
- **文字色**: `#ffffff`
- **字号**: 20-24px, font-weight: 700
- **字体**: DM Sans / heading font
- **字距**: letter-spacing: 2-3px
- **内边距**: 14-18px
- **底部装饰**: 3px solid `{{accent}}`（分隔标题与正文）

## 文字内容

| 字段 | 说明 |
|------|------|
| text | 主标题文字（如 "作业手册标准文档"） |

## 使用场景

- 所有文档模板必有段落
- 标题文字由模板的 `title` 字段指定
