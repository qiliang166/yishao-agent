# 一勺笔录(SOP)智能体 — 全部错误分类汇总表

**日期**: 2026-06-24 | **统计周期**: 2026-06-22 10:05 ~ 2026-06-23 22:37 (约 45 小时)

---

## 总体数据

| 指标 | 数值 |
|------|------|
| 会话总 Token 消耗（主会话） | **24,378,346** (输入 16.2M + 输出 8.1M) |
| 子 Agent 调度次数 | **41** 次（token 另计，未含在主会话中） |
| 工具调用总次数 | **7,220** (含 41 次 Agent 调度) |
| 用户消息数 | **717** (含 64 条 bug 报告 + 41 条回归报告) |
| 总提交数 | 121 |
| `fix:` 提交占比 | 55/121 = **45%** |
| UI 同一组件反复调整 | 模板弹窗 **17 次** + 缩略图 **6 次** |

---

## 一、全部错误分类明细表

### 类别 A：回归 Bug（改 A 导致 B 损坏）

| # | 错误名称 | 严重度 | 耗时估 | 涉及提交数 | Token 估 | 发生日期 | 错误现象 | 直接原因 | 根本原因 |
|---|----------|--------|--------|------------|----------|----------|----------|----------|----------|
| A1 | 保存按钮"已保存"错显（空内容） | 中 | ~45min | 1 (`c6862c0`) | ~80万 | 06-24 | 空文本框保存按钮显示"✓ 已保存"，用户无法区分保存状态 | `"" !== (savedSteps[key]\|\|"")` 两边空字符串相等 | 修改 `getSaveBtnLabel` 时未覆盖空内容边界情况 |
| A2 | 保存按钮全局联动闪烁 | **高** | ~1.5h | 1 (`ec629fb`) | ~120万 | 06-24 | 任意一个保存按钮点击后，全部 8+ 个按钮同时显示"已保存" | `savedFlash` 是全局共享状态，`getSaveBtnLabel` 将其作为最高优先级判断 | 未列出 8+ 个保存按钮消费者清单；将视觉反馈（CSS class）与数据状态（label text）耦合 |
| A3 | 模型选择器联动覆盖 | **严重** | ~2h | 2 (`31a2684`→`36a6ff1`) | ~180万 | 06-24 | Stage 2 选择任一模型，其余 6 个模型选择器全部变成相同值 | `31a2684` 将独立模型守卫合并为统一 `hasModelOverride`，用同一 `defVal` 覆盖全部 | 未理解原设计的分布式守卫意图；简化逻辑时破坏了每个子栏目的独立性 |
| A4 | 批量生成无结果反馈 | **严重** | ~1h | 1 (`20600d8`) | ~90万 | 06-24 | 点击"生成所有文案"显示"全部成功"，实际 Stage 2 无任何产出 | `doGenerate` 内部 catch 返回 null，`Promise.all([null,null,null])` 未逐项检查 | 调用可返回 null 的函数后未检查结果；"假成功"反馈误导用户 |
| A5 | AI 生成按钮无加载状态 | 中 | ~30min | 1 (`20600d8`) | ~50万 | 06-24 | Stage 2/4 生成按钮点击后无"生成中"视觉反馈 | `setGenerating(true)` 后未用 `try/finally` 确保状态恢复 | 异步状态管理模式不完整 |
| A6 | c4-dao SKILL 数据被覆盖 | **严重** | ~3h | 1 (`c6862c0`) | ~250万 | 06-24 | 用户 2705 字自定义 SKILL 被系统用 110 字摘要静默覆盖 | `PUT /api/column-configs/{id}` 无条件重新生成 skill | 自动化覆写逻辑缺少"用户数据保护"条件；需要数据库备份恢复 |
| A7 | PPT 模板中文名下载 500 | **高** | ~1h | 1 (`9519148`) | ~80万 | 06-24 | 下载 `道与术 · 默认模板.pptx` 时报 UnicodeEncodeError 500 | Starlette FileResponse Content-Disposition 头使用 latin-1 编码 | 未考虑非 ASCII 文件名场景 |
| A8 | 按钮独立性反复修复 | **高** | ~3h | 5 (`cadd82a`/`fefbe89`/`0c983b1`/`69e1bc2`/`64a9c4c`) | ~250万 | 06-23 | Stage 1/2 多个数据源/生成按钮互相干扰禁用 | 组件设计初期未规划独立状态管理 | 每次新增数据源都需要回头修按钮独立性 |
| A9 | listProviders 默认值覆盖 | 中 | ~45min | 1 (`31a2684`) | ~60万 | 06-23 | 加载 LLM provider 列表时默认值覆盖用户已选模型 | 初始化逻辑没有检查用户是否已手动选择 | 同上 A3 的模式——默认值逻辑未考虑用户已操作的情况 |

**类别 A 合计**：9 个回归 Bug | 估耗时 ~13h | Token ~1160 万 | 提交 14 个

---

### 类别 B：UI 反复调整（交互方案在代码中探索）

| # | 组件 | 迭代次数 | 耗时估 | Token 估 | 涉及提交 | 调整历程 |
|---|------|----------|--------|----------|----------|----------|
| B1 | 模板管理器弹窗架构 | 3 次重大变更 | ~5h | ~500万 | 17 | 行内展开编辑器 → 下拉展开 → 弹窗+拖拽+缩放 |
| B2 | 弹窗拖拽/缩放 | 4 次迭代 | ~1.5h | ~150万 | 4 (`671a3fa`/`3187414`/`18970e7`/`2584598`) | 硬编码尺寸 → useEffect 清理 → 防拖出屏幕 → 读真实 DOM |
| B3 | 缩略图间距/CSS方案 | 6 次尝试 | ~2h | ~180万 | 6 (`a2df125`→`a881269`→`2ee071d`→`47a6845`→`f819123`→`8428b0c`) | 5 种 CSS 方案才找到正确实现 |
| B4 | 弹窗布局（宽高/列/字体） | 5 次微调 | ~1.5h | ~120万 | 5 (`7a24f6c`/`74cea6d`/`8817b2f`/`c0c0107`/`75294cb`) | 2列/等宽字体/文本框高度/11px规范 |
| B5 | 弹窗字段增删 | 3 次调整 | ~1h | ~80万 | 3 (`2a11828`/`9b575a3`/`5447371`) | 模型选择器位置→移除冗余字段→缩略图位置 |
| B6 | 卡片布局+字体系统 | 2 次 | ~45min | ~60万 | 2 (`e2ef45a`/`c0c0107`) | 卡片溢出修复+统一字体规范 |

**类别 B 合计**：6 个组件/方面 | 估耗时 ~12h | Token ~1090 万 | 提交 ~37 个

---

### 类别 C：Prompt/SKILL 质量迭代

| # | 问题 | 耗时估 | Token 估 | 提交数 | 说明 |
|---|------|--------|----------|--------|------|
| C1 | AI 输出含问候语（3次修复） | ~1.5h | ~120万 | 3 (`53741fc`/`2315af3`/`f2d7c66`) | 逐个子栏目添加"no preamble"指令 |
| C2 | 输出格式约束不足 | ~30min | ~40万 | 1 (`19c8a84`) | 研学文案需硬格式约束 |
| C3 | Prompt/SKILL 模板升级 | ~2h | ~200万 | 4 (`139ba96`/`81fc902`/`fb74818`/`9423fed`) | 分别升级 SOP/研学/道与术 三大模板 |
| C4 | 规则→Prompt+Skill 自动生成 | ~1h | ~90万 | 2 (`539d9e9`/`0033163`) | PPT 规则系统 → 自动生成 prompt+skill |
| C5 | 配色/字体硬编码移除 | ~30min | ~40万 | 1 (`76ef421`) | 约束纪律重构 |

**类别 C 合计**：估耗时 ~5.5h | Token ~490 万 | 提交 11 个

---

### 类别 D：基础设施/配置错误

| # | 问题 | 耗时估 | Token 估 | 提交数 | 说明 |
|---|------|--------|----------|--------|------|
| D1 | 缺少 `/api/settings` 端点 | ~30min | ~30万 | 1 (`6e33760`) | 前端设置页 404 |
| D2 | 启动脚本缺 venv 创建 | ~30min | ~30万 | 1 (`92dc20c`) | 依赖安装失败 |
| D3 | 忘记密码提示 DB 路径错误 | ~15min | ~15万 | 1 (`07157ae`) | `data/` → `backend/data/` |
| D4 | SHA256 哈希不匹配 | ~30min | ~30万 | 1 (`4cb4554`) | + window.confirm 替换 |
| D5 | PPTGenerateRequest 放错文件 | ~20min | ~20万 | 1 (`9519148`) | app.py → models.py |

**类别 D 合计**：估耗时 ~2h | Token ~125 万 | 提交 5 个

---

### 类别 E：代码组织 / 重构失误

| # | 问题 | 耗时估 | Token 估 | 提交数 | 说明 |
|---|------|--------|----------|--------|------|
| E1 | 移除提示词管理器被 revert | ~1h | ~90万 | 3 (`4a615c3`/`5695069`/`567a185`) | 重构后才发现功能需要保留 |
| E2 | 硬编码 STAGE2_MODELS → 动态 | ~30min | ~30万 | 1 (`eff24f9`) | 初期用硬编码列表 |
| E3 | PPTGenerateRequest 归属 | 含在 D5 | — | — | 模型定义散布在 app.py |

**类别 E 合计**：估耗时 ~1.5h | Token ~120 万 | 提交 4 个

---

### 类别 F：沟通与流程问题

| # | 问题描述 | 影响 | 表现形式 |
|---|----------|------|----------|
| F1 | 用户充当 QA 测试员 | 用户在一次会话中报告 6 个回归 bug（保存按钮 x2、模型联动、批量生成、加载状态、数据丢失） | 用户说："教学文档中的保存按钮不起作用了" → "文案提取的保存按钮也不起作用了" → "又来了新问题" → "这些都是以前解决了的问题" |
| F2 | "假成功"反馈误导 | 用户浪费时间排查"为什么 Stage 2 是空的" | `doBatchGenerate` 实际无产出但提示"三篇文案已全部生成" |
| F3 | 信任损耗 | 用户反复质疑修改的安全性 | "你是不是做了什么事情导致的" / "你是什么原因总是重复的犯错呢 这样非常影响我们的工作" |
| F4 | 缺乏自测流程 | 每次修改后未运行完整的回归测试 | 121 个提交、0 个自动化测试文件 |
| F5 | 修改范围认知不足 | 修改一个函数时不清楚影响范围 | 未在修改前 grep 所有引用、列出消费者清单 |
| F6 | 子 Agent 调度未检结果 | 41 次子 Agent 调度，某些产出未充分验证 | 部分子 Agent 返回的代码未经充分审查 |

---

## 二、汇总

### 按类别汇总

| 类别 | 错误数 | 估耗时 | Token 消耗 | 提交数 |
|------|--------|--------|------------|--------|
| **A. 回归 Bug** | 9 | ~13h | ~1,160 万 | 14 |
| **B. UI 反复调整** | 6 组 | ~12h | ~1,090 万 | ~37 |
| **C. Prompt 迭代** | 5 组 | ~5.5h | ~490 万 | 11 |
| **D. 基础设施** | 5 | ~2h | ~125 万 | 5 |
| **E. 代码组织** | 3 | ~1.5h | ~120 万 | 4 |
| **F. 沟通流程** | 6 | N/A | N/A | N/A |
| **合计** | **34** | **~34h** | **~2,985 万** | **~71** |

### 按严重度汇总

| 严重度 | 数量 | 典型错误 |
|--------|------|----------|
| **严重** | 3 | A3 模型联动覆盖、A4 批量生成假成功、A6 SKILL 数据丢失 |
| **高** | 4 | A2 保存按钮全局联动、A7 中文文件名 500、A8 按钮独立性、B1 弹窗架构反复 |
| **中** | 12 | A1/A5/A9、B2-B6、C1-C3、D1-D4、E1-E3 |
| **低** | 3 | D3/D5、C4 |

### 时间分配总览

```
回归 Bug 修复:    ████████████████████████████ 38%
UI 反复调整:      ██████████████████████████   35%
Prompt 迭代:      ████████████                 16%
基础设施修复:     ████                          6%
代码组织重构:     ███                           4%
──────────────────────────────────────────────────
新功能开发占比仅 ~23%（28 个 feat 提交 vs 121 总提交）
```

### Token 消耗总览

```
主会话 Token:     24,370,000
错误相关 Token:   ~29,850,000 (估，含子 Agent 部分)
子 Agent 调度:     41 次（token 另计，未完全统计）

注：子 Agent 每次调度约消耗 10-50 万 token，
    41 次调度估计额外消耗 400-2000 万 token
```

---

## 三、根因分类

| 根因类型 | 导致错误 | 占比 | 预防措施 |
|----------|----------|------|----------|
| **修改不追踪消费者** | A1/A2/A3/A4/A8/A9/E1 | 40% | 修改前 grep 所有引用，列出消费者清单 |
| **缺乏自动化测试** | A1-A9 全部 | 覆盖所有 | 至少添加冒烟测试覆盖 5 Stage 核心流程 |
| **UI 方案无原型验证** | B1-B6 全部 | 20% | 交互方案先用简单原型验证，再编写代码 |
| **异步状态管理不完整** | A3/A4/A5/A9 | 15% | 统一 try/finally + 函数式 setState 模式 |
| **边界条件未覆盖** | A1/A6/A7/D3/D4 | 15% | 对空值、Unicode、用户自定义数据等边界写保护 |
| **巨型组件边界模糊** | A2/A3/A8 | 10% | 拆分 ProjectPage.tsx（1517行）为独立 Stage 组件 |
