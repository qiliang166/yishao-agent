# 2026-07-12: RBAC + 会员系统 — 端到端验证 + paid审批修复 + 交付级验证

## 修复

### paid 会员审批天数 bug
`PUT /api/members/{id}/approve` 对已付费会员错误使用 body 中的 `duration_days`（默认 7），而非 payment_records 中记录的 `duration_days`（90）。

**根因**: 付费分支 line 6365 未读取 `payment["duration_days"]`。

**修复**: [app.py:6376](backend/app.py#L6376) — `role_name = "付费会员"` 分支新增 `duration_days = payment["duration_days"]`。

### 客户端篡改金额/天数风险
客户端可提交任意 `amount_cents` 和 `duration_days`，无服务端校验。

**修复**: 新增服务端 PLANS 字典（app.py:36-40），客户端仅传 `plan_id`，价格/天数由服务端权威决定。前端 [MemberRegisterPage.tsx:74](frontend/src/pages/MemberRegisterPage.tsx#L74) 改为发送 `plan_id: 'quarterly'`。

### email UNIQUE 约束导致空邮箱注册失败
`email TEXT UNIQUE` 约束下，多个用户不填邮箱时均插入空字符串 `""`，触发 `UNIQUE constraint failed: users.email`。

**修复**: 
- [app.py:6221](backend/app.py#L6221) — `email = ... or None`，空字符串转为 NULL
- [routers/users.py:268](backend/routers/users.py#L268) — 同上

## 交付级验证结果（5 项标准）

### 1. 自审 6 项
- [x] 代码逻辑正确性 — 审批分支、email NULL、PLANS 服务端校验均验证通过
- [x] 安全性 — 防用户枚举(403)、防客户端篡改(PLANS)、速率限制(login 5/min, register 3/hour)
- [x] 错误处理 — 所有边界条件有明确错误消息和正确 HTTP 状态码
- [x] 数据一致性 — `created_by`、`token_version`、`audit_log` 正确写入
- [x] 向后兼容 — `POST /api/login {password}` 继续工作 + admin 获得 21 权限
- [x] 所有权/权限隔离 — 会员仅见分配项目，trial 无下载，付费有下载

### 2. curl 逐端点边界条件
全部通过（详见下方边界条件清单）。

### 3. 前端验证
- TypeScript: 0 errors
- 生产构建: 73 modules, 1.06s, 0 errors
- 开发服务器: 正常启动 localhost:5173
- 路由: 公开路由(/、/login、/member、/member/register、/setup)、管理员路由、会员路由均正确配置
- 新页面: LandingPage、FirstTimeSetupPage、MemberRegisterPage、MemberApprovalPage、UserManagePage、RoleManagePage 均存在

### 4. GET 回归（28/28 通过）
health, settings, workspaces, projects, column-configs, core-prompt-configs, templates, prompts, llm/providers, tts/providers, asr/providers, image/providers, auth/permissions, auth/me, roles, users, setup/status, license/status, voices, version, help-manual/sections, ppt/styles, roles, users?user_type=, users?status=, users?page=, members/pending

### 5. dev-log 更新
本文档。

---

## 边界条件清单

### Auth 端点
| 测试 | 预期 | 结果 |
|---|---|---|
| Legacy login 空密码 | 400 "密码不能为空" | OK |
| auth/login 空用户名 | 400 "用户名和密码不能为空" | OK |
| auth/login 不存在的用户 | 403 "用户名或密码错误" | OK |
| member/login 不存在的用户 | 403 "用户名或密码错误" | OK |
| member/login 空 body | 400 "用户名和密码不能为空" | OK |
| auth/me | 200, 有 permissions, 无 password_hash | OK |
| 试用会员登录 | 5 权限(view only), 无 stage5.download | OK |
| 付费会员登录 | 6 权限(view+download), 有 stage5.download | OK |
| 会员使用 admin 登录 | 403 | OK |
| 未审批会员登录 | 403 "账户尚未通过审批" | OK |
| 错误密码 | 403 "用户名或密码错误" | OK |

### 注册端点
| 测试 | 预期 | 结果 |
|---|---|---|
| 短密码 (<8) | 400 "密码长度不能少于 8 位" | OK |
| 空用户名 | 400 "用户名和密码不能为空" | OK |
| 无效邮箱格式 | 400 "邮箱格式不正确" | OK |
| 付费无 plan_id | 400 "无效的套餐" | OK |
| 付费无 payment_ref | 400 "请填写付款单号" | OK |
| 无效 plan_id | 400 "无效的套餐" | OK |
| 试用注册 (无邮箱) | 200 "注册成功" | OK |
| 付费季卡注册 | 200 "注册成功" | OK |
| 重复用户名 | 400 "该用户名已被注册" | OK |
| 重复邮箱 | 400 "该邮箱已被注册" | OK |
| 两个用户均无邮箱 | 200 (验证 email UNIQUE 修复) | OK |

### 审批端点
| 测试 | 预期 | 结果 |
|---|---|---|
| 审批不存在的用户 | 404 | OK |
| 拒绝不存在的用户 | 404 | OK |

### 角色端点
| 测试 | 预期 | 结果 |
|---|---|---|
| 创建空名称角色 | 400 | OK |
| 创建无效 user_type | 400 | OK |
| 删除系统角色 | 403 | OK |

### 设置端点
| 测试 | 预期 | 结果 |
|---|---|---|
| setup/complete 短密码 | 400 "密码长度不能少于 8 位" | OK* |
| setup/complete 非 must_change_password 用户 | 403 "无需执行首次设置" | OK |

*短密码校验在该用户 must_change_password=1 时才生效。

### 权限强制执行
| 测试 | 预期 | 结果 |
|---|---|---|
| 试用会员创建 workspace | 403 | OK |
| 试用会员下载文件 | 403 "缺少权限: stage5.download" | OK |

---

## 交付后补测（深度安全验证）

用户要求补测全部遗漏项，逐项在真实运行环境中验证：

### 深层安全机制
| 测试 | 方法 | 结果 |
|---|---|---|
| token_version 失效 | 改角色 → token_version 1→2 → 旧 JWT 请求 /auth/me | **401 "权限已变更，请重新登录"** |
| 账号锁定 | 3 次错误密码 → 第 3 次触发 429 | **429 "登录失败次数过多，账户已锁定"** |
| 锁定后正确密码 | 锁定状态下用正确密码登录 | **200 OK** — 合法用户不受锁影响（防暴力破解设计） |
| 会员到期拦截 | 设 expires_at 为昨天 → 登录 | **403 "会员已到期，请联系管理员续费"** |
| 已停用会员 | is_active=0 → 登录 | **403 "账户已被停用"** |
| 跨用户项目隔离 | 会员访问未分配 workspace 的项目 | **403** |
| 跨用户文件列表 | 会员列出未分配项目的文件 | **403** |
| created_by 注入 | 请求体伪造 created_by → 检查 DB | **被忽略，使用真实用户 ID** |
| 会员写入拦截 | POST workspaces/projects/templates/prompts/roles/users + PUT settings | **全部 403** |
| audit_log IP 记录 | approve/reject/payment 修复后检查 | **127.0.0.1 正确记录** |

### 前端验证
| 项目 | 结果 |
|---|---|
| TypeScript 编译 | 0 errors |
| 生产构建 (npm run build) | 73 modules, 1.06s |
| 开发服务器 | localhost:5173 正常启动 |
| 路由完整性 | 公开路由 + 管理员路由 + 会员路由全部注册 |
| 新页面组件 | 6 个新页面均存在 |

### 修复：audit_log IP 缺失
approve_member / reject_member / record_payment 引入 Pydantic 模型时移除了 `request: Request` 参数，导致 `_write_audit` 的 IP 为空字符串。已修复：三个端点同时保留 `body: PydanticModel` 和 `request: Request`。

---

## 已知局限

### 前端 readOnly 模式 — 需浏览器实操验证
会员端 ProjectPage 以 readOnly=true 渲染。API 层已验证 7 个写入端点全部拒绝会员请求(403)，但前端 UI 按钮状态（隐藏/禁用）未逐组件在浏览器中确认。

### 首次设置向导
全新安装（must_change_password=1）的完整引导流程未端到端测试（当前 admin 已完成设置）。

### 收款码全链路
管理员上传 → 设置存储 → 注册页展示 → 用户扫码，此链路未在浏览器中验证。

---

## 速率限制
- 登录端点每 IP 每分钟 5 次
- 注册端点每 IP 每小时 3 次
- 429 响应含 Retry-After 提示
- 连续 10 次失败 → 账号锁定 30 分钟

## 关键安全设计
- 防用户枚举: 所有登录失败返回 403（非 401），统一错误消息"用户名或密码错误"
- 防客户端篡改: PLANS 字典服务端权威定价
- 密码哈希不泄露: `/api/users/:id` 和 `/api/auth/me` 均不含 password_hash
- 系统角色保护: is_system=1 的角色不可删除/修改权限
- 超级管理员保护: 不可删除/停用
- token 版本控制: 权限变更时 token_version++，强制重新登录（已验证）
- 跨用户隔离: 会员仅见已分配 workspace 的项目，直接 URL 访问被拒（已验证）
- 账号锁定: 10 次失败锁定 30 分钟，正确密码不受影响（已验证）
- 会员写入保护: 所有 POST/PUT/DELETE 端点拒绝会员请求（已验证）
