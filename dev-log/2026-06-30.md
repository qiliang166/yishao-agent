# 2026-06-30 — 结构性修复：彻底消除硬编码，实现列级个性/共性分离

## 改动

### 场景文件画布变量化（Fix 2a）
**问题**：`scenarios/_default/` 下 4 个 .md 文件硬编码 `1280×720`，导致 col3 (A4 794×1123) 的 prompt 指令与实际画布不一致。

**方案**：全部替换为 `{{canvas_w}}` / `{{canvas_h}}` 模板变量，由 `_load_scenario_file()` 统一替换。

**文件**：
- `scenarios/_default/svg-generator.md` — 8 处替换
- `scenarios/_default/bento-grid-layout.md` — 5 处替换（含 rule-of-thirds 计算公式）
- `scenarios/_default/design-system.md` — 5 处替换（含安全区边界计算）
- `scenarios/_default/reviewer.md` — 5 处替换（含 viewport 和容器宽度阈值）

### HTML 输出提示词列级化（Fix 2b）
`scenarios/_default/html-output.md` 已使用 `{{canvas_w}}`/`{{canvas_h}}` 占位符，`_load_html_output_prompt(column_id)` 支持 per-column 覆写链，`_stage2_html_per_slide` 调用处做统一替换。

### 其他修复确认
Fix 1/3/4/5 已在上一阶段完成，本轮验证通过：
- `_assemble_html_deck` 动态画布 + 竖版 body 白色背景（Fix 1）
- Regenerate 管道 column_id 全链路贯通（Fix 3）
- `_extract_outermost_div` 替代正则 DIV 计数（Fix 4）
- 模板编辑 TAB 中文 fallback + 后端 API 容错（Fix 5）

## 验证
- Python 语法检查（ppt_service.py / models.py / app.py）：通过
- TypeScript 类型检查（frontend）：通过
- Frontend 构建：通过
- 场景文件残留检查：0 处 `1280`/`720` 硬编码
- `_get_canvas_dimensions` 逻辑不变（带 column_id → DB 读取，无 column_id → 1280×720 默认）
