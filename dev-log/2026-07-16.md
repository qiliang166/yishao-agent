# 2026-07-16

## 去掉用户名 @ 前缀

12 处，7 个文件：`@{username}` → `{username}`

- MAdmin.tsx:658, 725, 771, 875
- MMe.tsx:219
- MemberApprovalPage.tsx:283, 359
- MemberCenterPage.tsx:167
- UserManagePage.tsx:548, 697, 829
- PaymentHistoryDialog.tsx:81

## HTML 下载移动端遮挡 — 回退错误修复

回退了两个错误 commit 的改动（`973eddd` + `ad1c87c`）：
- 移除 `minimum-scale=0.1`
- 移除 body `width: 100vw`
- 恢复 `transform: scale()` 替代 `zoom`
- 恢复 `translateX(offsetX)` 居中
- 恢复 `marginBottom` 间距调整

新增防护：
- `html { overflow-x: hidden }` — 移动端双重裁剪
- body `max-width: 100vw` — 防止撑开
- viewport `width=1280`（html_designer）/ `width={canvas_w}`（ppt_service）
- fit() 添加 `if (scale < 0.5) return` 防双缩放

**结果**：问题未解决，用户放弃继续修复。根因是 `transform: scale()` 不改变布局盒子尺寸，1280px 幻灯片在 375px 视口溢出，移动浏览器产生水平滚动条导致左侧被裁切。

## 确认计划任务完成

MAdmin.tsx 6 项、MMe.tsx 申请记录、MRegister.tsx 注册页 — 均在上次会话已完成。
